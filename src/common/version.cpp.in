#include "common/version.h"

#ifdef TWILIGHT_HAS_GIT_INFO

const char GIT_COMMIT[] = "@TWILIGHT_GIT_COMMIT@";
const long long GIT_DATE = @TWILIGHT_GIT_DATE@ll;

#else

const char GIT_COMMIT[] = "unknown";
const long long GIT_DATE = 0ll;

#endif